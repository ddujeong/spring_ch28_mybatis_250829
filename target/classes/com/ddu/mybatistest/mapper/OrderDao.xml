<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.ddu.mybatistest.dao.OrderDao">
	<select id="allUserOrderListDao" resultType="com.ddu.mybatistest.dto.UserDto" >
		SELECT u.userid, u.username, o.ordername 
		FROM springdb.user AS u 
		INNER JOIN springdb.order AS o ON u.userid = o.userid 
		WHERE u.userid=#{userid}
	</select>
</mapper>